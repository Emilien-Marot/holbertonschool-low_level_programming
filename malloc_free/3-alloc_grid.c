#include <stdlib.h>
#include "main.h"

/**
 * str_concat - blabla
 *
 * @width: abc
 * @height: def
 *
 * Description: blabla
 * Return: blabla
 */
int **alloc_grid(int width, int height)
{
	if (width <= 0 || height <= 0)
		return (NULL);
}
